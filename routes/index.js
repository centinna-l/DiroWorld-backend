// MARK: All your route imports here.

const authRoutes = require("./auth_routes");
const llmRoutes = require("./llm_routes");

module.exports = {
  // add all your routes here.
  authRoutes,
  llmRoutes,
};
